# このプロジェクトについて

Yahoo Financeから株価を取得するPythonツールです。ユーザーはこのチャットで
「〇〇（銘柄名 or コード）の株価を〇日分見せて」のように話しかけてきます。

## あなた（Claude Code）が行うこと

1. ユーザーの発言から「銘柄コード」と「日数」を読み取る
   - 銘柄名で言われた場合（例:「ユーネクストの株価」）はティッカーコードに変換する
   - 日本株は数字4桁のコードが基本（例: 9418）。`.T` の付与はスクリプト側が自動で行うので不要
   - 米国株はアルファベットのティッカー（例: AAPL, TSLA）
   - 日数が指定されなければ5日として実行する

2. 以下のコマンドを実行する

```bash
python get_stock.py <ティッカー> <日数>
```

例:
```bash
python get_stock.py 9418 10
python get_stock.py AAPL 5
```

3. 実行結果（表形式のテキスト）をそのままユーザーに分かりやすく伝える
   - 騰落（▲/▼）や増減率も一緒に伝える
   - 表にはEMA（指数平滑移動平均線、デフォルト5日・25日）も含まれるので、EMAトレンド行（短期優勢/劣勢）も併せて伝える
   - EMAの期間を変えたいと言われたら `--ema-short` `--ema-long` オプションを付けて再実行する
   - 必要に応じて、CSV保存が欲しいか確認し、欲しければ `--csv` オプションを付けて再実行する

4. 銘柄コードが見つからない旨のエラーが出た場合は、コードのスペルミスがないか
   ユーザーに確認する（東証銘柄は4桁の数字が基本）

## 注意事項

- このスクリプトは実行のたびに毎回Yahoo Financeへ通信するため、インターネット接続が必要です
- 依存ライブラリが無いと言われた場合は `pip install -r requirements.txt` を提案してください
